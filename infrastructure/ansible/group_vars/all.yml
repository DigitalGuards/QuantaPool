---
# QuantaPool Ansible Global Variables

# Environment
environment: testnet

# QRL network configuration
qrl_chain_id: 1337
qrl_network: testnet
qrl_rpc_url: "https://qrlwallet.com/api/qrl-rpc/testnet"

# Contract addresses (testnet v2.2)
stqrl_address: "QA2f23388d1e3986416A36d2Ef113850D6900b69C"
deposit_pool_address: "Q109d7C528a67b80eb638D4C85e7C4545ef9Bb9aC"
validator_manager_address: "QA5b6e85B7713670589e4eAf2F039380Ec2792c8C"

# Client versions
gqrl_version: "latest"
qrysm_version: "latest"
gqrl_repo: "https://github.com/theQRL/go-qrl"
qrysm_repo: "https://github.com/theQRL/qrysm"

# Paths
gqrl_data_dir: "/var/lib/gqrl"
qrysm_beacon_data_dir: "/var/lib/qrysm/beacon"
qrysm_validator_data_dir: "/var/lib/qrysm/validator"
jwt_secret_path: "/etc/quantapool/jwt.hex"
validator_keys_dir: "/opt/quantapool/validator-keys"
validator_wallet_dir: "/var/lib/qrysm/validator/wallet"
slashing_protection_db: "/var/lib/qrysm/validator/slashing-protection"

# Fee recipient (set to your address to receive priority fees). Must NOT be
# empty — qrysm-validator exits with "default fileConfig fee recipient is not
# a valid qrl address" otherwise. Default is the QuantaPool deployer; override
# in inventory if this node services a different pool.
fee_recipient_address: "Q2E13b52fd3cda0a57f9037856B7Df971074e2489"

# Validator graffiti
validator_graffiti: "QuantaPool"

# Security
ssh_port: 22
fail2ban_enabled: true

# Monitoring
grafana_admin_password: "{{ lookup('env', 'GRAFANA_ADMIN_PASSWORD') | default('changeme', true) }}"

# Alerting (optional - set via environment or override)
discord_webhook_url: "{{ lookup('env', 'DISCORD_WEBHOOK_URL') | default('', true) }}"
telegram_bot_token: "{{ lookup('env', 'TELEGRAM_BOT_TOKEN') | default('', true) }}"
telegram_chat_id: "{{ lookup('env', 'TELEGRAM_CHAT_ID') | default('', true) }}"
