.env
.env.*
.claude
CLAUDE.md

# Private ops docs (host IPs, runbooks) - keep local, not in public repo
docs/NODE-SETUP.md
docs/REDEPLOY-PLAN.md

# Terraform state and secrets - never commit
*.tfvars
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Generated from terraform output (real host IPs); template is tracked
infrastructure/ansible/inventory.ini

node_modules/
artifacts/
build/
ccagent
webapp/node_modules

# Validator keys and secrets (never commit!)
validator_keys/
keystore_password.txt
*.swp

# Foundry build artifacts
cache/
out/
findings/*
