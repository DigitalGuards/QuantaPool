apiVersion: 1

providers:
  - name: 'QuantaPool Dashboards'
    orgId: 1
    folder: 'QuantaPool'
    folderUid: 'quantapool'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
